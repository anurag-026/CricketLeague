import { LiveMatchArena } from '@/features/gameplay/components/LiveMatchArena'

/** Screen 4 — Live match arena (scoreboard, duel, hex input). */
export function MatchArenaPage() {
  return <LiveMatchArena />
}
